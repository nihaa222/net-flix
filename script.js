/*const englishButton = document.querySelector(".english");

englishButton.addEventListener("click", function (e) {
  e.preventDefault();
});*/

const screenHeight = window.innerHeight;
const screenHeightPercentage = (screenHeight / window.screen.height) * 100;
console.log(`Screen height: ${screenHeightPercentage}%`);
